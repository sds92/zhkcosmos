import { SectionTitle } from 'components'
import React from 'react'

type ContactsPageProps = {}

const ContactsPage = ({}: ContactsPageProps) => {
  return (
    <section>
        <SectionTitle bg title={`Контакты`}/>
    </section>
  )
}

export default ContactsPage